services:
  - type: web
    name: rehui-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python start_render.py
    plan: free
    branch: main
    autoDeploy: true
